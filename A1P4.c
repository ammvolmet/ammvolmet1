#include<stdio.h>
 int main()
{
    
    float r,a;
   printf("enter the radius of circle:\n");
   scanf("%f",&r);
   a=6.14*r;
   printf("Area of circle is %f m^2 having the radius %f",a, r);
   return 0;
}